name: Publish to npm

run-name: 'Publish ${{ github.event.release.tag_name || inputs.tag }}'

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to publish (e.g., v1.2.3)'
        required: true
        type: string

permissions:
  contents: write # Need write to create releases
  id-token: write # Required for OIDC

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name || inputs.tag }}
          fetch-depth: 0

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'pnpm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build package
        run: pnpm run build

      - name: Upgrade npm to latest (required for OIDC)
        run: npm install -g npm@latest

      - name: Set package version
        run: |
          TAG=${{ github.event.release.tag_name || inputs.tag }}
          VERSION=${TAG#v}
          echo "Setting package version to $VERSION"
          npm pkg set version=$VERSION

      - name: Publish to npm (using OIDC)
        run: |
          TAG=${{ github.event.release.tag_name || inputs.tag }}
          ./scripts/publish-main.sh "${TAG#v}"

      # The skill package carries the same SKILL.md file this repo holds and depends on the
      # CLI published above, so it takes that version rather than one of its own. Publishing
      # it from the same job is what keeps the two from drifting apart.
      - name: Publish the skill package
        working-directory: skills/pipecraft-cli
        run: |
          TAG=${{ github.event.release.tag_name || inputs.tag }}
          ../../scripts/publish-skill.sh "${TAG#v}"
